import fs from "fs";
import { Gpio } from "onoff";
// const readWindows = () => {
//   for (let window of config.windows) {
//     const win = new Gpio(window.pin, "in");
//     console.log(win.readSync());
//   }
// };
